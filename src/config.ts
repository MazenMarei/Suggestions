export default {
    token       :"", /// bot token
    prefix      : "-",
    Mongoose    :"", /// mongoose url
    debugMode   : true,
    suggesttChannel : "", /// suggest channel id to implement suggestion in it
    adminChannel : "" /// admin channel id to send suggestion requst in it
}
